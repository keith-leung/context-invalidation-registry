[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: exercises a real LLM endpoint; requires config.yaml with a real api_key. Skip when key unavailable.
addopts = -ra --strict-markers
